class Question {
  String? questionText;
  List<String> choice;
  String? questionAnswer;

  Question({this.questionText, required this.choice, this.questionAnswer});

  int sum(a, b) {
    return a + b;
  }
}
